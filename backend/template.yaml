AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: LifeLedger FastAPI backend with fail-closed production defaults and explicit local overrides.

Parameters:
  AppEnv:
    Type: String
    Default: production
    AllowedValues:
      - local
      - production
  AuthMode:
    Type: String
    Default: cognito
    AllowedValues:
      - local
      - cognito
  LocalDevUserId:
    Type: String
    Default: local-dev-user
  PersistenceMode:
    Type: String
    Default: dynamodb
    AllowedValues:
      - local
      - dynamodb
  RemindersTableName:
    Type: String
    Default: lifeledger-reminders-auth
  RecordsTableName:
    Type: String
    Default: lifeledger-records-auth
  PreferencesTableName:
    Type: String
    Default: lifeledger-preferences-auth
  PushSubscriptionsTableName:
    Type: String
    Default: lifeledger-push-subscriptions-auth
  GoogleCalendarConnectionsTableName:
    Type: String
    Default: lifeledger-google-calendar-connections-auth
  GoogleOAuthStatesTableName:
    Type: String
    Default: lifeledger-google-oauth-states-auth
  RecordAttachmentsTableName:
    Type: String
    Default: lifeledger-record-attachments-auth
  LinkedItemsTableName:
    Type: String
    Default: lifeledger-linked-items-auth
  SearchIndexTableName:
    Type: String
    Default: lifeledger-search-index-auth
  SavedViewsTableName:
    Type: String
    Default: lifeledger-saved-views-auth
  ResponsibilityHistoryTableName:
    Type: String
    Default: lifeledger-responsibility-history-auth
  DocumentStorageMode:
    Type: String
    Default: s3
    AllowedValues:
      - disabled
      - local
      - s3
  DocumentsUploadBucketName:
    Type: String
    Default: ""
  DocumentsCleanBucketName:
    Type: String
    Default: ""
  MalwareProtectionEnabled:
    Type: String
    Default: "true"
    AllowedValues:
      - "true"
      - "false"
  AttachmentMaxSizeBytes:
    Type: Number
    Default: 10485760
  AttachmentMaxPerRecord:
    Type: Number
    Default: 5
  GoogleClientId:
    Type: String
    Default: ""
  GoogleOAuthSecretArn:
    Type: String
    Default: ""
  GoogleOAuthRedirectUri:
    Type: String
    Default: ""
  GoogleCalendarScopes:
    Type: String
    Default: "https://www.googleapis.com/auth/calendar.events https://www.googleapis.com/auth/calendar.calendarlist.readonly"
  VapidPublicKey:
    Type: String
    Default: ""
  PushSecretArn:
    Type: String
    Default: ""
  VapidSubject:
    Type: String
    Default: ""
  RecordEncryptionMode:
    Type: String
    Default: kms
    AllowedValues:
      - disabled
      - local
      - kms
  CorsAllowedOrigins:
    Type: String
    Default: https://lifeledger.jpreinold.com,https://www.lifeledger.jpreinold.com

Rules:
  ProductionSecurityControls:
    RuleCondition: !Equals [!Ref AppEnv, production]
    Assertions:
      - Assert: !Equals [!Ref AuthMode, cognito]
        AssertDescription: Production requires Cognito authentication.
      - Assert: !Equals [!Ref PersistenceMode, dynamodb]
        AssertDescription: Production requires DynamoDB persistence.
      - Assert: !Equals [!Ref RecordEncryptionMode, kms]
        AssertDescription: Production requires KMS protected-record encryption.
      - Assert: !Equals [!Ref DocumentStorageMode, s3]
        AssertDescription: Production requires encrypted S3 document storage.
      - Assert: !Equals [!Ref MalwareProtectionEnabled, "true"]
        AssertDescription: Production requires document malware protection.

Conditions:
  HasGoogleOAuthSecretArn: !Not [!Equals [!Ref GoogleOAuthSecretArn, ""]]
  HasPushSecretArn: !Not [!Equals [!Ref PushSecretArn, ""]]
  HasDocumentsUploadBucketName: !Not [!Equals [!Ref DocumentsUploadBucketName, ""]]
  HasDocumentsCleanBucketName: !Not [!Equals [!Ref DocumentsCleanBucketName, ""]]
  EnableMalwareProtection: !Equals [!Ref MalwareProtectionEnabled, "true"]

Globals:
  Function:
    Runtime: python3.13
    Timeout: 30
    MemorySize: 512

Resources:
  LifeLedgerDataEncryptionKey:
    Type: AWS::KMS::Key
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Description: LifeLedger protected records and integration token encryption
      EnableKeyRotation: true
      PendingWindowInDays: 30
      KeyPolicy:
        Version: "2012-10-17"
        Statement:
          - Sid: EnableAccountAdministration
            Effect: Allow
            Principal:
              AWS: !Sub "arn:${AWS::Partition}:iam::${AWS::AccountId}:root"
            Action: "kms:*"
            Resource: "*"

  LifeLedgerDataEncryptionAlias:
    Type: AWS::KMS::Alias
    Properties:
      AliasName: !Sub "alias/${AWS::StackName}-data"
      TargetKeyId: !Ref LifeLedgerDataEncryptionKey

  LifeLedgerDocumentsKey:
    Type: AWS::KMS::Key
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Description: LifeLedger secure document storage encryption
      EnableKeyRotation: true
      PendingWindowInDays: 30
      KeyPolicy:
        Version: "2012-10-17"
        Statement:
          - Sid: EnableAccountAdministration
            Effect: Allow
            Principal:
              AWS: !Sub "arn:${AWS::Partition}:iam::${AWS::AccountId}:root"
            Action: "kms:*"
            Resource: "*"

  LifeLedgerDocumentsKeyAlias:
    Type: AWS::KMS::Alias
    Properties:
      AliasName: !Sub "alias/${AWS::StackName}-documents"
      TargetKeyId: !Ref LifeLedgerDocumentsKey

  DocumentsQuarantineBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !If [HasDocumentsUploadBucketName, !Ref DocumentsUploadBucketName, !Ref AWS::NoValue]
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        IgnorePublicAcls: true
        BlockPublicPolicy: true
        RestrictPublicBuckets: true
      OwnershipControls:
        Rules:
          - ObjectOwnership: BucketOwnerEnforced
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: aws:kms
              KMSMasterKeyID: !GetAtt LifeLedgerDocumentsKey.Arn
            BucketKeyEnabled: true
      CorsConfiguration:
        CorsRules:
          - AllowedOrigins: !Split [",", !Ref CorsAllowedOrigins]
            AllowedMethods:
              - POST
            AllowedHeaders:
              - Content-Type
              - x-amz-server-side-encryption
              - x-amz-server-side-encryption-aws-kms-key-id
            ExposedHeaders:
              - ETag
            MaxAge: 300
      LifecycleConfiguration:
        Rules:
          - Id: ExpireAbandonedQuarantineUploads
            Status: Enabled
            Prefix: quarantine/
            ExpirationInDays: 1
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 1

  DocumentsCleanBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      BucketName: !If [HasDocumentsCleanBucketName, !Ref DocumentsCleanBucketName, !Ref AWS::NoValue]
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        IgnorePublicAcls: true
        BlockPublicPolicy: true
        RestrictPublicBuckets: true
      OwnershipControls:
        Rules:
          - ObjectOwnership: BucketOwnerEnforced
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: aws:kms
              KMSMasterKeyID: !GetAtt LifeLedgerDocumentsKey.Arn
            BucketKeyEnabled: true
      CorsConfiguration:
        CorsRules:
          - AllowedOrigins: !Split [",", !Ref CorsAllowedOrigins]
            AllowedMethods:
              - GET
              - HEAD
            AllowedHeaders:
              - "*"
            ExposedHeaders:
              - Content-Length
              - Content-Type
              - Content-Disposition
              - ETag
            MaxAge: 300

  DocumentsQuarantineBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref DocumentsQuarantineBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: DenyInsecureTransport
            Effect: Deny
            Principal: "*"
            Action: "s3:*"
            Resource:
              - !GetAtt DocumentsQuarantineBucket.Arn
              - !Sub "${DocumentsQuarantineBucket.Arn}/*"
            Condition:
              Bool:
                aws:SecureTransport: false
          - Sid: DenyUnapprovedObjectEncryption
            Effect: Deny
            Principal: "*"
            Action: "s3:PutObject"
            Resource: !Sub "${DocumentsQuarantineBucket.Arn}/quarantine/*"
            Condition:
              StringNotEquals:
                s3:x-amz-server-side-encryption: aws:kms
          - Sid: DenyUnapprovedKmsKey
            Effect: Deny
            Principal: "*"
            Action: "s3:PutObject"
            Resource: !Sub "${DocumentsQuarantineBucket.Arn}/quarantine/*"
            Condition:
              StringNotEquals:
                s3:x-amz-server-side-encryption-aws-kms-key-id: !GetAtt LifeLedgerDocumentsKey.Arn

  DocumentsCleanBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref DocumentsCleanBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: DenyInsecureTransport
            Effect: Deny
            Principal: "*"
            Action: "s3:*"
            Resource:
              - !GetAtt DocumentsCleanBucket.Arn
              - !Sub "${DocumentsCleanBucket.Arn}/*"
            Condition:
              Bool:
                aws:SecureTransport: false
          - Sid: DenyUnapprovedObjectEncryption
            Effect: Deny
            Principal: "*"
            Action: "s3:PutObject"
            Resource: !Sub "${DocumentsCleanBucket.Arn}/*"
            Condition:
              StringNotEquals:
                s3:x-amz-server-side-encryption: aws:kms
          - Sid: DenyUnapprovedKmsKey
            Effect: Deny
            Principal: "*"
            Action: "s3:PutObject"
            Resource: !Sub "${DocumentsCleanBucket.Arn}/*"
            Condition:
              StringNotEquals:
                s3:x-amz-server-side-encryption-aws-kms-key-id: !GetAtt LifeLedgerDocumentsKey.Arn

  LifeLedgerUserPool:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: !Sub "${AWS::StackName}-users"
      UsernameAttributes:
        - email
      AutoVerifiedAttributes:
        - email
      AdminCreateUserConfig:
        AllowAdminCreateUserOnly: true
      AccountRecoverySetting:
        RecoveryMechanisms:
          - Name: verified_email
            Priority: 1
      Policies:
        PasswordPolicy:
          MinimumLength: 12
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: false
          RequireUppercase: true
      Schema:
        - Name: email
          AttributeDataType: String
          Mutable: true
          Required: true

  LifeLedgerUserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: !Sub "${AWS::StackName}-web-client"
      UserPoolId: !Ref LifeLedgerUserPool
      GenerateSecret: false
      PreventUserExistenceErrors: ENABLED
      ExplicitAuthFlows:
        - ALLOW_USER_SRP_AUTH
        - ALLOW_USER_PASSWORD_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH
      SupportedIdentityProviders:
        - COGNITO

  LifeLedgerHttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      CorsConfiguration:
        AllowOrigins: !Split [",", !Ref CorsAllowedOrigins]
        AllowMethods:
          - GET
          - POST
          - PUT
          - PATCH
          - DELETE
          - OPTIONS
        AllowHeaders:
          - Content-Type
          - Authorization
          - Idempotency-Key
        AllowCredentials: true
      Auth:
        DefaultAuthorizer: CognitoJwtAuthorizer
        Authorizers:
          CognitoJwtAuthorizer:
            IdentitySource: "$request.header.Authorization"
            JwtConfiguration:
              issuer: !Sub "https://cognito-idp.${AWS::Region}.amazonaws.com/${LifeLedgerUserPool}"
              audience:
                - !Ref LifeLedgerUserPoolClient

  LifeLedgerApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-api"
      CodeUri: .
      Handler: lambda_handler.handler
      Environment:
        Variables:
          APP_ENV: !Ref AppEnv
          APP_COMPONENT: api
          AUTH_MODE: !Ref AuthMode
          COGNITO_USER_POOL_ID: !Ref LifeLedgerUserPool
          COGNITO_USER_POOL_CLIENT_ID: !Ref LifeLedgerUserPoolClient
          LOCAL_DEV_USER_ID: !Ref LocalDevUserId
          PERSISTENCE_MODE: !Ref PersistenceMode
          REMINDERS_TABLE_NAME: !Ref RemindersTable
          RECORDS_TABLE_NAME: !Ref RecordsTable
          RECORD_ATTACHMENTS_TABLE_NAME: !Ref RecordAttachmentsTable
          LINKED_ITEMS_TABLE_NAME: !Ref LinkedItemsTable
          SEARCH_INDEX_TABLE_NAME: !Ref SearchIndexTable
          SAVED_VIEWS_TABLE_NAME: !Ref SavedViewsTable
          RESPONSIBILITY_HISTORY_TABLE_NAME: !Ref ResponsibilityHistoryTable
          PREFERENCES_TABLE_NAME: !Ref PreferencesTable
          PUSH_SUBSCRIPTIONS_TABLE_NAME: !Ref PushSubscriptionsTable
          GOOGLE_CALENDAR_CONNECTIONS_TABLE_NAME: !Ref GoogleCalendarConnectionsTable
          GOOGLE_OAUTH_STATES_TABLE_NAME: !Ref GoogleOAuthStatesTable
          LOCAL_DATA_FILE: /tmp/lifeledger-reminders.json
          LOCAL_RECORDS_FILE: /tmp/lifeledger-records.json
          LOCAL_RECORD_ATTACHMENTS_FILE: /tmp/lifeledger-record-attachments.json
          LOCAL_LINKED_ITEMS_FILE: /tmp/lifeledger-linked-items.json
          LOCAL_SEARCH_INDEX_FILE: /tmp/lifeledger-search-index.json
          LOCAL_SAVED_VIEWS_FILE: /tmp/lifeledger-saved-views.json
          LOCAL_RESPONSIBILITY_HISTORY_FILE: /tmp/lifeledger-responsibility-history.json
          LOCAL_PREFERENCES_FILE: /tmp/lifeledger-preferences.json
          LOCAL_PUSH_SUBSCRIPTIONS_FILE: /tmp/lifeledger-push-subscriptions.json
          LOCAL_GOOGLE_CALENDAR_CONNECTIONS_FILE: /tmp/lifeledger-google-calendar-connections.json
          LOCAL_GOOGLE_OAUTH_STATES_FILE: /tmp/lifeledger-google-oauth-states.json
          GOOGLE_CLIENT_ID: !Ref GoogleClientId
          GOOGLE_OAUTH_SECRET_ARN: !Ref GoogleOAuthSecretArn
          GOOGLE_OAUTH_REDIRECT_URI: !Ref GoogleOAuthRedirectUri
          GOOGLE_CALENDAR_SCOPES: !Ref GoogleCalendarScopes
          VAPID_PUBLIC_KEY: !Ref VapidPublicKey
          PUSH_SECRET_ARN: !Ref PushSecretArn
          VAPID_SUBJECT: !Ref VapidSubject
          DATA_ENCRYPTION_KMS_KEY_ARN: !GetAtt LifeLedgerDataEncryptionKey.Arn
          RECORD_ENCRYPTION_MODE: !Ref RecordEncryptionMode
          DOCUMENT_STORAGE_MODE: !Ref DocumentStorageMode
          DOCUMENTS_QUARANTINE_BUCKET: !Ref DocumentsQuarantineBucket
          DOCUMENTS_CLEAN_BUCKET: !Ref DocumentsCleanBucket
          DOCUMENTS_KMS_KEY_ARN: !GetAtt LifeLedgerDocumentsKey.Arn
          ATTACHMENT_MAX_SIZE_BYTES: !Ref AttachmentMaxSizeBytes
          ATTACHMENT_MAX_PER_RECORD: !Ref AttachmentMaxPerRecord
          ALLOW_PLAINTEXT_PRODUCTION_SECRETS: "false"
          CORS_ALLOWED_ORIGINS: !Ref CorsAllowedOrigins
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref RemindersTable
        - DynamoDBCrudPolicy:
            TableName: !Ref RecordsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref RecordAttachmentsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref LinkedItemsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref SearchIndexTable
        - DynamoDBCrudPolicy:
            TableName: !Ref SavedViewsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref ResponsibilityHistoryTable
        - DynamoDBCrudPolicy:
            TableName: !Ref PreferencesTable
        - DynamoDBCrudPolicy:
            TableName: !Ref PushSubscriptionsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref GoogleCalendarConnectionsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref GoogleOAuthStatesTable
        - Statement:
            - Effect: Allow
              Action:
                - kms:GenerateDataKey
                - kms:Decrypt
              Resource: !GetAtt LifeLedgerDataEncryptionKey.Arn
              Condition:
                StringEquals:
                  kms:EncryptionContext:app: lifeledger
        - Statement:
            - Effect: Allow
              Action:
                - s3:PutObject
                - s3:GetObject
                - s3:DeleteObject
                - s3:GetObjectTagging
              Resource: !Sub "${DocumentsQuarantineBucket.Arn}/quarantine/*"
            - Effect: Allow
              Action:
                - s3:PutObject
                - s3:GetObject
                - s3:DeleteObject
                - s3:PutObjectTagging
              Resource: !Sub "${DocumentsCleanBucket.Arn}/clean/*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !GetAtt LifeLedgerDocumentsKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: !Sub "s3.${AWS::Region}.amazonaws.com"
        - Statement:
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !GetAtt LifeLedgerDataEncryptionKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: dynamodb.*.amazonaws.com
            - Effect: Allow
              Action:
                - kms:CreateGrant
              Resource: !GetAtt LifeLedgerDataEncryptionKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: dynamodb.*.amazonaws.com
                Bool:
                  kms:GrantIsForAWSResource: true
        - !If
          - HasGoogleOAuthSecretArn
          - Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource: !Ref GoogleOAuthSecretArn
          - !Ref AWS::NoValue
        - !If
          - HasPushSecretArn
          - Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource: !Ref PushSecretArn
          - !Ref AWS::NoValue
      Events:
        Health:
          Type: HttpApi
          Properties:
            ApiId: !Ref LifeLedgerHttpApi
            Path: /health
            Method: GET
            Auth:
              Authorizer: NONE
        OptionsProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref LifeLedgerHttpApi
            Path: /{proxy+}
            Method: OPTIONS
            Auth:
              Authorizer: NONE
        ApiProxy:
          Type: HttpApi
          Properties:
            ApiId: !Ref LifeLedgerHttpApi
            Path: /{proxy+}
            Method: ANY

  LifeLedgerDigestPushFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-digest-push"
      CodeUri: .
      Handler: digest_push_handler.handler
      Environment:
        Variables:
          APP_ENV: !Ref AppEnv
          APP_COMPONENT: digest
          AUTH_MODE: !Ref AuthMode
          COGNITO_USER_POOL_ID: !Ref LifeLedgerUserPool
          COGNITO_USER_POOL_CLIENT_ID: !Ref LifeLedgerUserPoolClient
          LOCAL_DEV_USER_ID: !Ref LocalDevUserId
          PERSISTENCE_MODE: !Ref PersistenceMode
          REMINDERS_TABLE_NAME: !Ref RemindersTable
          RECORDS_TABLE_NAME: !Ref RecordsTable
          PREFERENCES_TABLE_NAME: !Ref PreferencesTable
          PUSH_SUBSCRIPTIONS_TABLE_NAME: !Ref PushSubscriptionsTable
          LOCAL_DATA_FILE: /tmp/lifeledger-reminders.json
          LOCAL_RECORDS_FILE: /tmp/lifeledger-records.json
          LOCAL_PREFERENCES_FILE: /tmp/lifeledger-preferences.json
          LOCAL_PUSH_SUBSCRIPTIONS_FILE: /tmp/lifeledger-push-subscriptions.json
          VAPID_PUBLIC_KEY: !Ref VapidPublicKey
          PUSH_SECRET_ARN: !Ref PushSecretArn
          VAPID_SUBJECT: !Ref VapidSubject
          ALLOW_PLAINTEXT_PRODUCTION_SECRETS: "false"
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref RemindersTable
        - DynamoDBCrudPolicy:
            TableName: !Ref PreferencesTable
        - DynamoDBCrudPolicy:
            TableName: !Ref PushSubscriptionsTable
        - Statement:
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !GetAtt LifeLedgerDataEncryptionKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: dynamodb.*.amazonaws.com
            - Effect: Allow
              Action:
                - kms:CreateGrant
              Resource: !GetAtt LifeLedgerDataEncryptionKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: dynamodb.*.amazonaws.com
                Bool:
                  kms:GrantIsForAWSResource: true
        - !If
          - HasPushSecretArn
          - Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:GetSecretValue
                Resource: !Ref PushSecretArn
          - !Ref AWS::NoValue
      Events:
        DailyDigestSchedule:
          Type: Schedule
          Properties:
            Schedule: rate(15 minutes)
            Enabled: true

  RemindersTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref RemindersTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: id
          KeyType: RANGE

  RecordsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref RecordsTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: id
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: id
          KeyType: RANGE

  ResponsibilityHistoryTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref ResponsibilityHistoryTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: event_id
          AttributeType: S
        - AttributeName: reminder_partition
          AttributeType: S
        - AttributeName: item_partition
          AttributeType: S
        - AttributeName: idempotency_hash
          AttributeType: S
        - AttributeName: event_sort
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: event_id
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: ReminderHistoryIndex
          KeySchema:
            - AttributeName: reminder_partition
              KeyType: HASH
            - AttributeName: event_sort
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: ItemActivityIndex
          KeySchema:
            - AttributeName: item_partition
              KeyType: HASH
            - AttributeName: event_sort
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: HistoryIdempotencyIndex
          KeySchema:
            - AttributeName: idempotency_hash
              KeyType: HASH
            - AttributeName: event_id
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  RecordAttachmentsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref RecordAttachmentsTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: record_attachment_key
          AttributeType: S
        - AttributeName: owner_hash
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: record_attachment_key
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: OwnerHashRecordAttachmentIndex
          KeySchema:
            - AttributeName: owner_hash
              KeyType: HASH
            - AttributeName: record_attachment_key
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  LinkedItemsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref LinkedItemsTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: link_id
          AttributeType: S
        - AttributeName: source_link_key
          AttributeType: S
        - AttributeName: target_link_key
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: link_id
          KeyType: RANGE
      GlobalSecondaryIndexes:
        - IndexName: SourceLinksIndex
          KeySchema:
            - AttributeName: user_id
              KeyType: HASH
            - AttributeName: source_link_key
              KeyType: RANGE
          Projection:
            ProjectionType: ALL
        - IndexName: TargetLinksIndex
          KeySchema:
            - AttributeName: user_id
              KeyType: HASH
            - AttributeName: target_link_key
              KeyType: RANGE
          Projection:
            ProjectionType: ALL

  SearchIndexTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref SearchIndexTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: search_key
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: search_key
          KeyType: RANGE

  SavedViewsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref SavedViewsTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: saved_view_id
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: saved_view_id
          KeyType: RANGE

  PreferencesTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref PreferencesTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH

  PushSubscriptionsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref PushSubscriptionsTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: subscription_id
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: subscription_id
          KeyType: RANGE

  GoogleCalendarConnectionsTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref GoogleCalendarConnectionsTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH

  GoogleOAuthStatesTable:
    Type: AWS::DynamoDB::Table
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      TableName: !Ref GoogleOAuthStatesTableName
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true
        SSEType: KMS
        KMSMasterKeyId: !GetAtt LifeLedgerDataEncryptionKey.Arn
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: true
      AttributeDefinitions:
        - AttributeName: state
          AttributeType: S
      KeySchema:
        - AttributeName: state
          KeyType: HASH

  GuardDutyDocumentsMalwareProtectionRole:
    Type: AWS::IAM::Role
    Condition: EnableMalwareProtection
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: malware-protection-plan.guardduty.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: GuardDutyDocumentsMalwareProtectionPolicy
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Sid: AllowManagedRuleToSendS3EventsToGuardDuty
                Effect: Allow
                Action:
                  - events:PutRule
                  - events:DeleteRule
                  - events:PutTargets
                  - events:RemoveTargets
                Resource: !Sub "arn:${AWS::Partition}:events:${AWS::Region}:${AWS::AccountId}:rule/DO-NOT-DELETE-AmazonGuardDutyMalwareProtectionS3*"
                Condition:
                  StringLike:
                    events:ManagedBy: malware-protection-plan.guardduty.amazonaws.com
              - Sid: AllowGuardDutyToMonitorEventBridgeManagedRule
                Effect: Allow
                Action:
                  - events:DescribeRule
                  - events:ListTargetsByRule
                Resource: !Sub "arn:${AWS::Partition}:events:${AWS::Region}:${AWS::AccountId}:rule/DO-NOT-DELETE-AmazonGuardDutyMalwareProtectionS3*"
              - Sid: AllowPostScanTag
                Effect: Allow
                Action:
                  - s3:PutObjectTagging
                  - s3:GetObjectTagging
                  - s3:PutObjectVersionTagging
                  - s3:GetObjectVersionTagging
                Resource: !Sub "${DocumentsQuarantineBucket.Arn}/quarantine/*"
              - Sid: AllowEnableS3EventBridgeEvents
                Effect: Allow
                Action:
                  - s3:PutBucketNotification
                  - s3:GetBucketNotification
                Resource: !GetAtt DocumentsQuarantineBucket.Arn
              - Sid: AllowPutValidationObject
                Effect: Allow
                Action:
                  - s3:PutObject
                Resource: !Sub "${DocumentsQuarantineBucket.Arn}/malware-protection-resource-validation-object"
              - Sid: AllowCheckBucketOwnership
                Effect: Allow
                Action:
                  - s3:ListBucket
                Resource: !GetAtt DocumentsQuarantineBucket.Arn
              - Sid: AllowMalwareScan
                Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectVersion
                Resource: !Sub "${DocumentsQuarantineBucket.Arn}/quarantine/*"
              - Sid: AllowDecryptForMalwareScan
                Effect: Allow
                Action:
                  - kms:GenerateDataKey
                  - kms:Decrypt
                Resource: !GetAtt LifeLedgerDocumentsKey.Arn
                Condition:
                  StringLike:
                    kms:ViaService: !Sub "s3.${AWS::Region}.amazonaws.com"

  DocumentsMalwareProtectionPlan:
    Type: AWS::GuardDuty::MalwareProtectionPlan
    Condition: EnableMalwareProtection
    Properties:
      Role: !GetAtt GuardDutyDocumentsMalwareProtectionRole.Arn
      ProtectedResource:
        S3Bucket:
          BucketName: !Ref DocumentsQuarantineBucket
          ObjectPrefixes:
            - quarantine/
      Actions:
        Tagging:
          Status: ENABLED
      Tags:
        - Key: Application
          Value: LifeLedger

  LifeLedgerAttachmentScanFinalizerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "${AWS::StackName}-attachment-scan-finalizer"
      CodeUri: .
      Handler: attachment_scan_finalizer.handler
      Environment:
        Variables:
          APP_ENV: !Ref AppEnv
          APP_COMPONENT: attachment_finalizer
          AUTH_MODE: !Ref AuthMode
          COGNITO_USER_POOL_ID: !Ref LifeLedgerUserPool
          COGNITO_USER_POOL_CLIENT_ID: !Ref LifeLedgerUserPoolClient
          LOCAL_DEV_USER_ID: !Ref LocalDevUserId
          PERSISTENCE_MODE: !Ref PersistenceMode
          REMINDERS_TABLE_NAME: !Ref RemindersTable
          RECORDS_TABLE_NAME: !Ref RecordsTable
          RECORD_ATTACHMENTS_TABLE_NAME: !Ref RecordAttachmentsTable
          LINKED_ITEMS_TABLE_NAME: !Ref LinkedItemsTable
          SEARCH_INDEX_TABLE_NAME: !Ref SearchIndexTable
          LOCAL_DATA_FILE: /tmp/lifeledger-reminders.json
          LOCAL_RECORDS_FILE: /tmp/lifeledger-records.json
          LOCAL_RECORD_ATTACHMENTS_FILE: /tmp/lifeledger-record-attachments.json
          LOCAL_LINKED_ITEMS_FILE: /tmp/lifeledger-linked-items.json
          LOCAL_SEARCH_INDEX_FILE: /tmp/lifeledger-search-index.json
          DOCUMENT_STORAGE_MODE: !Ref DocumentStorageMode
          DOCUMENTS_QUARANTINE_BUCKET: !Ref DocumentsQuarantineBucket
          DOCUMENTS_CLEAN_BUCKET: !Ref DocumentsCleanBucket
          DOCUMENTS_KMS_KEY_ARN: !GetAtt LifeLedgerDocumentsKey.Arn
          ATTACHMENT_MAX_SIZE_BYTES: !Ref AttachmentMaxSizeBytes
          ATTACHMENT_MAX_PER_RECORD: !Ref AttachmentMaxPerRecord
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref RemindersTable
        - DynamoDBCrudPolicy:
            TableName: !Ref RecordsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref RecordAttachmentsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref LinkedItemsTable
        - DynamoDBCrudPolicy:
            TableName: !Ref SearchIndexTable
        - Statement:
            - Effect: Allow
              Action:
                - s3:GetObject
                - s3:DeleteObject
                - s3:GetObjectTagging
              Resource: !Sub "${DocumentsQuarantineBucket.Arn}/quarantine/*"
            - Effect: Allow
              Action:
                - s3:PutObject
                - s3:GetObject
                - s3:DeleteObject
                - s3:PutObjectTagging
              Resource: !Sub "${DocumentsCleanBucket.Arn}/clean/*"
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !GetAtt LifeLedgerDocumentsKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: !Sub "s3.${AWS::Region}.amazonaws.com"
        - Statement:
            - Effect: Allow
              Action:
                - kms:Encrypt
                - kms:Decrypt
                - kms:ReEncrypt*
                - kms:GenerateDataKey*
                - kms:DescribeKey
              Resource: !GetAtt LifeLedgerDataEncryptionKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: dynamodb.*.amazonaws.com
            - Effect: Allow
              Action:
                - kms:CreateGrant
              Resource: !GetAtt LifeLedgerDataEncryptionKey.Arn
              Condition:
                StringEquals:
                  kms:CallerAccount: !Ref AWS::AccountId
                StringLike:
                  kms:ViaService: dynamodb.*.amazonaws.com
                Bool:
                  kms:GrantIsForAWSResource: true

  AttachmentScanResultRule:
    Type: AWS::Events::Rule
    Properties:
      EventPattern:
        source:
          - aws.guardduty
        detail-type:
          - GuardDuty Malware Protection Object Scan Result
        detail:
          resourceType:
            - S3_OBJECT
          s3ObjectDetails:
            bucketName:
              - !Ref DocumentsQuarantineBucket
      State: ENABLED
      Targets:
        - Arn: !GetAtt LifeLedgerAttachmentScanFinalizerFunction.Arn
          Id: LifeLedgerAttachmentScanFinalizer

  AttachmentScanResultInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref LifeLedgerAttachmentScanFinalizerFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt AttachmentScanResultRule.Arn

Outputs:
  DataEncryptionKeyArn:
    Description: KMS key ARN for LifeLedger application-level and DynamoDB encryption.
    Value: !GetAtt LifeLedgerDataEncryptionKey.Arn
  DataEncryptionKeyAlias:
    Description: KMS alias for LifeLedger application data encryption.
    Value: !Ref LifeLedgerDataEncryptionAlias
  DocumentsKeyArn:
    Description: KMS key ARN for LifeLedger secure document storage.
    Value: !GetAtt LifeLedgerDocumentsKey.Arn
  DocumentsKeyAlias:
    Description: KMS alias for LifeLedger secure document storage.
    Value: !Ref LifeLedgerDocumentsKeyAlias
  DocumentsQuarantineBucketName:
    Description: Private S3 bucket for untrusted quarantined document uploads.
    Value: !Ref DocumentsQuarantineBucket
  DocumentsCleanBucketName:
    Description: Private S3 bucket for scanned and validated record attachments.
    Value: !Ref DocumentsCleanBucket
  RecordAttachmentsTableName:
    Description: DynamoDB record attachments metadata table name.
    Value: !Ref RecordAttachmentsTable
  MalwareProtectionPlanId:
    Description: GuardDuty Malware Protection plan ID when enabled.
    Value: !If [EnableMalwareProtection, !GetAtt DocumentsMalwareProtectionPlan.MalwareProtectionPlanId, "disabled"]
  ApiUrl:
    Description: HTTP API endpoint for the LifeLedger backend.
    Value: !Sub "https://${LifeLedgerHttpApi}.execute-api.${AWS::Region}.amazonaws.com/"
  RemindersTable:
    Description: DynamoDB reminders table name.
    Value: !Ref RemindersTable
  RecordsTable:
    Description: DynamoDB records table name.
    Value: !Ref RecordsTable
  PreferencesTable:
    Description: DynamoDB user preferences table name.
    Value: !Ref PreferencesTable
  LinkedItemsTable:
    Description: DynamoDB linked items table name.
    Value: !Ref LinkedItemsTable
  SearchIndexTable:
    Description: DynamoDB unified search index table name.
    Value: !Ref SearchIndexTable
  SavedViewsTable:
    Description: DynamoDB saved search views table name.
    Value: !Ref SavedViewsTable
  ResponsibilityHistoryTable:
    Description: DynamoDB responsibility lifecycle history table name.
    Value: !Ref ResponsibilityHistoryTable
  PushSubscriptionsTable:
    Description: DynamoDB push subscriptions table name.
    Value: !Ref PushSubscriptionsTable
  GoogleCalendarConnectionsTable:
    Description: DynamoDB Google Calendar connections table name.
    Value: !Ref GoogleCalendarConnectionsTable
  GoogleOAuthStatesTable:
    Description: DynamoDB Google OAuth states table name.
    Value: !Ref GoogleOAuthStatesTable
  UserPoolId:
    Description: Cognito User Pool ID.
    Value: !Ref LifeLedgerUserPool
  UserPoolClientId:
    Description: Cognito User Pool app client ID for the frontend.
    Value: !Ref LifeLedgerUserPoolClient

